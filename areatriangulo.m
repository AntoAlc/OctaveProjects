function areatriangulo  
  clear;
  clc;
  %reales (b, h, A)
  disp ('ingrese el valor de la base');
  b=input('');
  disp('ingrese el valor de la altura:');
  h=input (' ');
  A=b*h/2 ;
  disp('el valor del área es:'); disp (A);

end 